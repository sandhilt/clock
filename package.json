{
  "name": "clock",
  "version": "1.0.0",
  "description": "This is a example of clock in Vanilla",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "serve -s ."
  },
  "author": "Bruno Ochotorena",
  "dependencies": {
    "normalize.css": "^8.0.0",
    "web-animations-js": "^2.3.1"
  },
  "devDependencies": {
    "serve": "^10.0.2"
  }
}
